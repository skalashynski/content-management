package com.productiveedge.content_mgmt_automation.entity.tag;

public class TagHelper {
    /*
    public static CompoundTag cloneParentTag(CompoundTag target) {
        CompoundTag clone = new CompoundTag();
        if (target != null) {
            clone.setName(target.getName());
            clone.setHtmlContent(target.getHtmlContent());
            clone.setTextContent(target.getTextContent());
            clone.setPageUrl(target.getPageUrl());
            clone.setFullXPath(target.getFullXPath());
        }
        return clone;
    }
    */
}
